# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/


# $('#name, #address, #email').on('blur keypress', -> 
#     nameLength = $('#name').val().length;
#     addressLength = $('#address').val().length;
#     emailLength = $('email').val().length;
#     if(nameLength == 0 || addressLength == 0 || emailLength == 0)
#         $('.actions').prop('disable', true);
#     else 
#         $('.actions').prop('disable', false);
    
# );


# $('.actions').on('blur keyup', ()=> {
#     if($("#name").valid()) {
#         $('.actions').prop('disabled', false);  
#     } else {
#         $('.actions').prop('disabled', 'disabled');
#     };
# });